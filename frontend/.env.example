# Apps Script web app URL (member profile lookup)
# Get from: Apps Script editor → Deploy → Manage deployments → copy Web app URL
# After setting this, also restrict the deployment in Google:
#   Deploy → Manage deployments → Edit → "Who has access" → Members of your org
VITE_APPS_SCRIPT_URL=
